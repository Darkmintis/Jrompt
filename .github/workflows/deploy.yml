name: Deploy Jrompt to darkmintis-dev

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Jrompt repository
        uses: actions/checkout@v4

      - name: Checkout darkmintis-dev
        uses: actions/checkout@v4
        with:
          repository: Darkmintis/darkmintis-dev
          token: ${{ secrets.DEPLOY_PAT }}
          path: main-repo

      - name: Copy production files to main repo
        run: |
          # Remove old deployment
          rm -rf main-repo/dist/jrompt
          mkdir -p main-repo/dist/jrompt
          
          # Copy only production files
          cp index.html main-repo/dist/jrompt/
          cp style.css main-repo/dist/jrompt/
          cp survey.css main-repo/dist/jrompt/
          cp -r js main-repo/dist/jrompt/
          cp LICENSE main-repo/dist/jrompt/
          cp README.md main-repo/dist/jrompt/
          
          # Copy .gitignore if exists
          [ -f .gitignore ] && cp .gitignore main-repo/dist/jrompt/ || true
          
          echo "✅ Production files copied successfully"
          echo "📦 Deployed files:"
          ls -la main-repo/dist/jrompt/

      - name: Commit and push to darkmintis-dev
        run: |
          cd main-repo
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          
          # Add changes
          git add dist/jrompt
          
          # Commit only if there are changes
          if git diff --staged --quiet; then
            echo "ℹ️ No changes to deploy"
          else
            git commit -m "🔄 Update Jrompt from commit ${GITHUB_SHA:0:7}"
            git push
            echo "✅ Changes pushed successfully"
          fi

      - name: Deployment summary
        run: |
          echo "## 🎉 Deployment Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Tool**: Jrompt" >> $GITHUB_STEP_SUMMARY
          echo "- **Live URL**: https://darkmintis.dev/jrompt" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: \`${GITHUB_SHA:0:7}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered by**: ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Time**: $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Deployed Files" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ index.html" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ style.css" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ survey.css" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ js/ (all modules)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ LICENSE" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ README.md" >> $GITHUB_STEP_SUMMARY
